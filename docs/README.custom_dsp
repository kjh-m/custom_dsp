This is the custom_dsp-write-a-block package meant as a guide to building
out-of-tree packages. To use the custom_dsp blocks, the Python namespaces
is in 'custom_dsp', which is imported as:

    import custom_dsp

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(custom_dsp)
